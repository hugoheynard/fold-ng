/** Surface shape of a `fold-button-icon`. */
export type FoldButtonIconShape = "square" | "round";

/** Hit-area preset — `xs` 22px · `sm` 28px · `md` 32px · `lg` 38px. */
export type FoldButtonIconSize = "xs" | "sm" | "md" | "lg";

/** Visual tone — colour + background. */
export type FoldButtonIconTone = "ghost" | "accent" | "critical";
